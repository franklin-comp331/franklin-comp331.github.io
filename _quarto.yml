project:
  type: website

# Default rendering for site pages (docs, index, etc.) should be standard HTML
format:
  html:
    toc: true
    # Add html-specific options here if desired (theme, number-sections, etc.)

# Note: Slides for lectures are configured via a directory-level metadata file
# in lectures/_metadata.yml so that only lecture notebooks render with revealjs.

